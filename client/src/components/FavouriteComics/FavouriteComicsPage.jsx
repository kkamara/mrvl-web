import React from "react"
import { APP_NAME, } from "../../constants"

const FavouriteComicsPage = () => {
	const pageTitle = `Favourite Comics | ${APP_NAME}`

	return (
		<p>FavouriteComicsPage</p>
	)
}

export default FavouriteComicsPage
